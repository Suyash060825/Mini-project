# --- Hybrid Video Summarizer Configuration ---

# 1. API KEY for Cloud Services (OpenAI)
# If left empty, Cloud Transcription (Whisper API) and GPT-4o Summarization modes will not work.
# Replace the placeholder with your actual OpenAI API Key if you want to use Cloud features.


# 2. Ollama Configuration (Used for Local Summarization)
# This URL allows the backend container to reach the Ollama container running on the host network.
OLLAMA_URL=http://host.docker.internal:11434

# 3. Model Name (Ensure this model is pulled in the Ollama container)
# Must match the model you pulled in the setup step (e.g., 'mistral', 'llama3').
OLLAMA_MODEL=mistral